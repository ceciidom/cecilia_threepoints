name: CI/CD Pipeline

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. Ejecución de pruebas de calidad de código (Pruebas de SAST)
      - name: Pruebas de SAST
        run: echo "Ejecución de pruebas de SAST"

      # 3. Crear un archivo de credenciales
      - name: Crear archivo de credenciales
        run: |
          echo "[credentials]" > credentials.ini
          echo "user=${{ secrets.USERNAME }}" >> credentials.ini
          echo "password=${{ secrets.PASSWORD }}" >> credentials.ini

      # 4. Construcción del container de Docker
      - name: Build docker image
        run: docker build -t devops_ws:$(date +%s) .
